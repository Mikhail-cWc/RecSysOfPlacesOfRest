# 1. Скопируйте этот файл: cp env.example .env
# 2. Отредактируйте .env и укажите реальные значения для переменных,
#    помеченных как "ОБЯЗАТЕЛЬНО" или "РЕКОМЕНДУЕТСЯ"
# 3. Все значения по умолчанию указаны в backend/app/core/config.py

# API
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=True
API_BASE_URL=http://backend:8000

# POSTGRESQL
POSTGRES_HOST=postgres
POSTGRES_PORT=5435
POSTGRES_DB=places_db
POSTGRES_USER=places_user
POSTGRES_PASSWORD=places_password  # ОБЯЗАТЕЛЬНО изменить!

# QDRANT
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_GRPC_PORT=6334
QDRANT_COLLECTION=places

# REDIS
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
SESSION_TTL=86400  # Время жизни сессии в секундах (24 часа)

# LLM
LLM_BASE_URL=https://openrouter.ai/api/v1
OPENAI_API_KEY= # ОБЯЗАТЕЛЬНО изменить!
OPENAI_MODEL=google/gemini-2.5-flash
OPENAI_TEMPERATURE=0.7

# EMBEDDINGS
OPENAI_EMBEDDING_BASE_URL=https://openrouter.ai/api/v1
OPENAI_EMBEDDING_MODEL=baai/bge-m3
OPENAI_EMBEDDING_DIM=1024

# TELEGRAM BOT
TELEGRAM_BOT_TOKEN= # ОБЯЗАТЕЛЬНО изменить!


JWT_SECRET_KEY= # ОБЯЗАТЕЛЬНО: Используйте случайную строку openssl rand -hex 32!
JWT_ALGORITHM=HS256

# Токен для внутренних запросов от бота к API
BOT_API_TOKEN= # ОБЯЗАТЕЛЬНО: Используйте случайную строку openssl rand -hex 32!

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# PHOENIX TRACING
PHOENIX_ENABLED=true
PHOENIX_ENDPOINT=http://phoenix:6006/v1/traces
PHOENIX_PROJECT_NAME=places-recommendation-agent
